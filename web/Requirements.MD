Create a nodeJS web UI for prompting neo4j as knowledge graph (use env credentials). 
The UI should have a field for asking a question. 
Each question and answer should be saved inside a local storage.
The prompt history should be displayed below the question field as a card. Only display the latest 3 questions/answers if there are more display clickable "Load more" label.
Use local ollama gemma4-26b model for prompting knowledge graph. (check for installed ollama model before continuing)